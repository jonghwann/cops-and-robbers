import { Text } from 'react-native';
import Screen from '@/components/layout/screen';

export default function SavedMeetings() {
  return (
    <Screen>
      <Text>saved-meetings</Text>
    </Screen>
  );
}
